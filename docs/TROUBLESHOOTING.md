# Troubleshooting & FAQ

### 1. Expo Go crashes on startup
Ensure dependencies match your Expo SDK version via `npx expo install --check`.

### 2. Invalid WakaTime API Key
Verify that the key has read permissions for summaries and stats.
